import React from "react";
import { connect } from "frontity";

const Archives = ({ state, actions }) => {
  return (
    <>
      <h1>To jest archiwum</h1>
    </>
  );
};

export default connect(Archives);
